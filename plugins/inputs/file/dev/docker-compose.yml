version: '3'

services:
  telegraf:
      image: glinton/scratch
      volumes:
        - ./telex.conf:/telex.conf
        - ../../../../telegraf:/telegraf
        - ./dev/json_a.log:/var/log/test.log
      entrypoint:
        - /telegraf
        - --config
        - /telex.conf
