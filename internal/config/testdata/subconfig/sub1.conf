# Read flattened metrics from one or more commands that output JSON to stdout
[[inputs.exec]]
  # the command to run
  command = "/usr/bin/uname -a"
  name_suffix = "_uname1"
  data_format = "value"
  data_type = "string"
